{
    "name": "@polkar/dashboard-builder-assistant",
    "description": "Library for implementing a dashboard builder assistant, which looks for the correct visualizations, and configures their parameters based on the user prompt",
    "version": "0.0.1",
    "private": true,
    "dependencies": {},
    "devDependencies": {
    "typescript": "^5.3.3"
},
    "scripts": {
        "build": "echo 'No build script provided'",
        "dev": "echo 'No dev script provided'",
        "export": "echo 'No export script provided'",
        "preview": "echo 'No preview script provided'"
    }
}